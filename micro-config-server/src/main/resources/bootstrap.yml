spring:
  application:
    name: configserver
  profiles:
    active: composite
  cloud:
    config:
      server:
    #    prefix:  /config
        composite:
          - type: native
            search-locations: ${HOME}/Documents/projects/java/microServices/micro-config-server/configuration
        bootstrap: true
       
       
server:
  port: 9002
  