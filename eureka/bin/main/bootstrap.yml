spring:
  application:
    name: discovery
  cloud:
    config:
     # name: configuration-server
      uri: http://localhost:9002
      
management:
  endpoints:
    web:
      exposure:
        include: refresh
  